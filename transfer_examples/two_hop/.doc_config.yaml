# configuration for conversion to docs.globus.org
title: 'Two Stage Globus Transfer'
short_description: |
    Transfer from source to intermediate and then from intermediate to
    destination. Remove from intermediate after completion.

example_dir: 'two_hop'
index_source:
  copy: "README.adoc"
append_source_blocks: true

menu_weight: 200
