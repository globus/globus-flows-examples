# configuration for conversion to docs.globus.org
title: 'Transfer After Approval'
short_description: |
    Allow users to submit an approval request to transfer a file to a
    destination Guest Collection.

example_dir: 'transfer_after_approval'
index_source:
  copy: "README.adoc"
append_source_blocks: true

menu_weight: 300
